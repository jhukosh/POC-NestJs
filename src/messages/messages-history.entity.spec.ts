import { MessagesHistoryEntity } from './messages-history.entity';

describe('MessagesHistoryEntity', () => {
  it('should be defined', () => {
    expect(new MessagesHistoryEntity()).toBeDefined();
  });
});
